name: 🚀 Publish Package

on:
  push:
    tags:
      - 'v*'  # Запускать workflow только для тегов, начинающихся с "v"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v2

    - name: 🐍 Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.12'  # Укажите нужную версию Python

    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install setuptools wheel twine

    - name: 🔧 Build package
      run: python setup.py sdist bdist_wheel

    - name: 🌐 Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        username: __token__  # Используйте токен для аутентификации
        password: ${{ secrets.PYPI_API_TOKEN }}  # Добавьте ваш токен в Secrets репозитория
